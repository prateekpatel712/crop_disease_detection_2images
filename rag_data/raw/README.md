# Raw RAG Data

Add source material for disease knowledge retrieval here in a later phase.

Examples:

- PDF references
- CSV files
- disease notes
- agronomy guidance documents

Phase 4 retrieval prefers structured JSON or JSONL records in either `raw/` or
`processed/`, but `processed/` should be treated as the primary retrieval source.
