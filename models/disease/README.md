# Disease Model Slot

Add trained disease-detection model artifacts here in a later phase.

Recommended future contents:

- model weights such as `.pt`, `.pth`, or `.onnx`
- label map files such as `labels.txt` or `labels.json`
- optional crop-specific subfolders like `tomato/` or `rice/`
