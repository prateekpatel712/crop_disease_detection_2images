# Model Metadata Slot

Use this directory for shared model metadata in later phases.

Examples:

- class names
- preprocessing configuration
- threshold configuration
- version notes
- optional `disease_routes.json` crop-to-model routing metadata
